# Day 1

Did this with Kotlin today; with the Kotlin compiler installed, just run `run.sh $FILE` to run it with a specific input file. If not specified, it will use `input.txt` in the same directory.

---

Typical day 1 question, nice and chill. Did this with Kotlin for fun, no idea what theme I'll do for 2019 yet though.

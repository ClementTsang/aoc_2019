# Day 3

This wasn't too hard though I got stuck at part two because of a dumb logic bug, where the test inputs all passed but the actual input didn't :/

To run, do `go run ./day_03/main.go $INPUT_FILE`.
